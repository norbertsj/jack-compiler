class Main {
    field int x, y; 
    field int size;

    
    method void draw() {
        do Screen.setColor(true);
        do Screen.drawRectangle(x, y, 1 - (x + size), 23 * (y + size));
        return;
    }
}
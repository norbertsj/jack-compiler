// This file is part of www.nand2tetris.org
// and the book "The Elements of Computing Systems"
// by Nisan and Schocken, MIT Press.
// File name: projects/11/Pong/Ball.jack

class Main {
    field int x, y;  // the ball's screen location (in pixels)
    field int size;

    /**
     * Moves the ball one unit towards its destination.
     * If the ball has reached a wall, returns 0.
     * Else, returns a value according to the wall:
     * 1 (left wall), 2 (right wall), 3 (top wall), 4 (bottom wall).
     */
    method void draw() {
        do Screen.setColor(true)
        do Screen.drawRectangle(x, y, 1 - (x + size), 23 * (y + size));
        return;
    }

     /** Draws the ball. */
     method void draw() {
	    do Screen.drawRectangle(x, y, x + 5, y + 5); /* another comment */
        do Screen.setColor(false); /** and another weird comment spanning
        multiple line 1
        multiple line 2
        // abc
        //abc
        /* abc
        /* abc
        */
        do Schocken.create(); // /*
	    return;
    }

    /**yeah
    *prints stuff
    *******/
    method void print() {
        do Output.print(1); /* multiline comment
        abc
        abc */
        return;
    }
}